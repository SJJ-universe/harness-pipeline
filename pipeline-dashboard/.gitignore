node_modules/
.harness/
runs/
*.log
npm-debug.log*
# Tooling caches (Apr 27): uv (Python package manager) leaves a 22k+ file
# tree under .uv-cache that bloats every codebase scan. Same idea applies
# to other resolver / build caches that may appear next to package.json.
.uv-cache/
.cache/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.parcel-cache/
.next/
.nuxt/
.turbo/
.svelte-kit/
coverage/
.nyc_output/
.env
.env.*
# Slice S1 (Phase 3-S): doc templates are not real secrets — keep tracked.
!.env.example
!.env.sample
!.env.template
# Slice R2-1 (Phase D R2, 2026-04-28): R2 deployment evaluation env
# template. The filled-in copy (.env.r2) stays ignored.
!.env.r2.example
dist/
build/
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
